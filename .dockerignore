node_modules
.next
.git
.github
.superpowers
docs
tests
test-results
playwright-report
*.md
# Not recursive by default: `.env` alone would only exclude the one at the root and
# would let docker/.env — the file with the production password — into the image.
**/.env
**/.env.*
!**/.env.example
