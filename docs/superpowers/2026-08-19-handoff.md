# Handoff — 19 de agosto de 2026

Sesión larga. Se cerró el Plan A completo, se pasó la revisión de rama y se diseñó el
ciclo 3. Nada quedado a medias en el código: el árbol está limpio y todo verde.

## Qué pasó hoy

**Plan A terminado.** Las cuatro tareas que faltaban, cada una con TDD y verificación
real en navegador o en la pila Docker:

| Tarea | Commit | Qué entrega |
|---|---|---|
| 11 Hero 3D | `8b82252` | Sobre interactivo, póster como LCP, degradación por capacidad |
| 12 Leads | `0200c0d` | Consulta validada, persistencia, enlaces de WhatsApp por plan |
| 13 SEO | `0fa86d4` | hreflang, JSON-LD, sitemap, robots, imagen Open Graph |
| 14 Despliegue | `30d9705` | Docker, Caddy con TLS, respaldos, 11 pruebas e2e |

**Revisión completa de rama** (`d3a63b6`): tres revisores en paralelo sobre
`17f3099..30d9705`. Cuatro hallazgos críticos, todos reales y todos corregidos — entre
ellos que `docker/.env` entraba en la imagen y que el limitador de peticiones era
eludible con una cabecera. El detalle completo, con su motivo, está en el ledger:
`.superpowers/sdd/2026-08-18-marketing-site-plan-a/progress.md`, sección "Revisión
completa de rama".

**Diseño del ciclo 3** (`f1a0b0e`): spec del motor de invitaciones y RSVP en
`docs/superpowers/specs/2026-08-19-invitation-engine-design.md`. **Pendiente de que el
usuario lo revise.**

## Estado verificable

- Rama `feat/sitio-publico`, 6 commits por delante de `main`, árbol limpio, **sin fusionar**.
- 164 pruebas unitarias + 11 e2e verdes. `pnpm typecheck` y `pnpm lint` sin errores.
- Pila Docker completa probada: los cuatro servicios sanos, TLS por Caddy, precios
  leídos de Postgres, degradación comprobada parando el contenedor de la base.

## Lo que descubrió esta sesión y conviene no olvidar

**El spec del ciclo 1 no se cumplió entero.** Su sección 2 mete dentro del alcance
"pedido con subida de comprobante" y "panel de administración mínimo". El Plan A los
empujó a un "Plan B" que nunca se escribió. Falta también toda la autenticación: hoy no
existe ni un `route.ts`, ni sesiones, ni usuarios. El sitio vende; no fabrica ni administra.

**Al depurar el hero perdí tiempo persiguiendo una regresión inexistente**: la escena 3D
aparecía en blanco y las secciones atenuadas porque la pestaña de Chrome estaba en
segundo plano (`visibilityState: "hidden"`), y ahí no disparan ni IntersectionObserver
ni ResizeObserver. Si vuelve a pasar, comprobar eso **antes** de tocar código.

## Decisiones del usuario para el ciclo 3

Tomadas en esta sesión, ya reflejadas en el spec:

- Plataforma multi-inquilino: un despliegue, N eventos.
- La unidad invitada es el **grupo con cupos**, no la persona.
- Token por invitado, guardado solo como hash.
- Canales de envío: WhatsApp asistido, email, copiar/CSV y QR — todos en la rebanada 2.
- API de WhatsApp aplazada; la copia del sitio ya se ajustó para no prometerla.
- Panel solo para el atelier; el cliente recibe enlace de solo lectura.
- La invitación se compone a mano y el motor le inyecta el RSVP. Sin editor visual.
- Backend como módulos con capas dentro de la misma app, no servicio aparte.

Descomposición acordada: **1 núcleo** (esta rebanada) · 2 canales · 3 puerta con QR ·
4 refinamientos.

## Qué hacer a continuación

1. **Pedir al usuario que revise el spec** del motor de invitaciones. Es la puerta que
   quedó abierta: no empieces el plan sin su visto bueno.
2. Con el spec aprobado, invocar `superpowers:writing-plans` para la rebanada 1.
3. **Primera tarea de ese plan, obligatoria**: reintroducir el layout raíz no dinámico.
   El panel vive fuera de `/[locale]` y en la Task 8 el layout raíz se fusionó con
   `[locale]`. Está anotado en el ledger desde entonces.
4. Decidir con el usuario si fusionar `feat/sitio-publico` a `main` antes de empezar
   el ciclo 3 o seguir sobre la misma rama.

## Pendientes del usuario, sin resolver

- **Datos reales** en `src/shared/config/brand.ts`: WhatsApp, dominio y email siguen
  siendo marcadores.
- **Estadísticas inventadas** en `src/sections/StatsStrip.tsx`: "320+ eventos",
  "94% confirmación RSVP", "7 países". Vienen del diseño Ivory y se publican como
  hechos sobre su negocio. Se le señaló; no eligió qué hacer.
- **Testimonios**: solo Daniela Ortiz es real; los otros dos son de relleno.
- **Fotos** de las plantillas `zafiro` y `onix`: marcadores generados.
- **Póster del hero**: SVG compuesto, no captura del primer fotograma.
- Datos de transferencia y QR de pago, que necesita el Plan B.

## Diferido a propósito (motivo en el ledger)

Caché sobre los repositorios, captura de UTM, prueba del repositorio Drizzle de
consultas contra base real, y verificar en producción que Caddy entrega la IP correcta
al limitador.

## Skills para la próxima sesión

- `superpowers:writing-plans` — plan de la rebanada 1, en cuanto el spec esté aprobado.
- `superpowers:subagent-driven-development` — para ejecutarlo, como en el Plan A.
- `superpowers:finishing-a-development-branch` — si se decide fusionar antes de seguir.

## Nota de estilo

El usuario escribe en español y tiene el modo caveman activo: respuestas comprimidas,
sin relleno. Avísale al cerrar cada tarea; no pidas permiso entre tareas de un plan ya
aprobado. Es directo cuando algo no le cuadra — respóndele con hechos verificados, no
con conjeturas.
